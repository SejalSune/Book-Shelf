<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Book - BookShelf</title>
    <style>
        body {
            font-family: sans-serif; /* Playful cursive font */
            margin: 0;
            padding: 20px;
            background-color: #f7f9f9; /* Light pastel background */
        }
        /* Center "Book Details" heading horizontally */
        h1 {
            font-size: 2em;
            margin-bottom: 15px;
            color: #333; /* Darker text for better contrast */
            text-shadow: 1px 1px 1px rgba(255, 255, 255, 0.5); /* Subtle text shadow for depth */
            text-align: center;
            width: 100%;
            margin-top: 0;
        }
        .book-details {
            display: flex;
            flex-wrap: wrap;
            justify-content: space-around; /* Center details and image horizontally */
            margin: 0 auto; /* Center the content section */
            max-width: 700px; /* Limit container width for smaller screens */
            border: 5px solid #e0e0e0; /* Cute border around details */
            border-radius: 10px; /* Rounded corners */
            padding: 20px;
            background-color: #fff; /* White background for details */
            margin-bottom: 9px;
        }
        .book-details div {
            width: calc(50% - 10px); /* Adjusted width for two-column layout with margin */
            margin-bottom: 10px; /* Space between each label-input pair */
        }
        .book-details div:nth-child(odd) {
            margin-right: 20px; /* Right margin for odd-numbered divs */
        }
        .book-details label {
            display: block;
            margin-bottom: 5px;
            color: #333; /* Darker text for labels */
        }
        .book-details input[type="text"],
        .book-details input[type="date"],
        .book-details input[type="number"] {
            width: calc(100% - 10px); /* Adjusted width for input fields */
            padding: 8px;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
        }
        .book-details textarea {
            width: calc(100% - 10px); /* Adjusted width for textarea */
            padding: 8px;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
        }
        button {
            width: 100%;
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            background-color: #007bff;
            color: #fff;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }
        button:hover {
            background-color: #0056b3;
        }

        /* Responsive styling for form */
        @media (max-width: 768px) {
            .book-details div {
                width: 100%; /* Full width for smaller screens */
                margin-right: 0; /* No margin for smaller screens */
            }
            .book-details div:nth-child(odd) {
                margin-right: 0; /* No margin for odd-numbered divs on smaller screens */
            }
            textarea {
                height: 80px;
            }
        }
    </style>
</head>
<body>
    <form action="/shelf/<%= bookdata._id %>?_method=PUT" method="post">
        <div class="book-details">
            <h1>Add book info</h1>
            <div>
                <label for="title">Title:</label>
                <input type="text" id="title" name="book[title]" value="<%= bookdata.title %>" required>
            </div>
            <div>
                <label for="author">Author:</label>
                <input type="text" id="author" name="book[author]" value="<%= bookdata.author %>" required>
            </div>
            <div>
                <label for="language">Language:</label>
                <input type="text" id="language" name="book[language]" value="<%= bookdata.language %>" required>
            </div>
            <div>
                <label for="publisher">Publisher:</label>
                <input type="text" id="publisher" name="book[publisher]" value="<%= bookdata.publisher %>" required>
            </div>
            <div>
                <label for="pages">Pages:</label>
                <input type="text" id="pages" name="book[pages]" value="<%= bookdata.pages %>" required>
            </div>
            <div>
                <label for="publication_date">Publication Date:</label>
                <input type="date" id="publication_date" name="book[publication_date]" value="<%= bookdata.publication_date ? bookdata.publication_date.toISOString().split('T')[0] : '' %>" required>
            </div>            
            <div>
                <label for="ISBN">ISBN:</label>
                <input type="number" id="ISBN" name="book[ISBN]" value="<%= bookdata.ISBN %>" required>
            </div>
            <div>
                <label for="coverpage">Cover Page:</label>
                <input type="text" id="coverpage" name="book[coverpage]" value="<%= bookdata.coverpage %>" required>
            </div>
            <div>
                <label for="price">Price:</label>
                <input type="text" id="price" name="book[price]" value="<%= bookdata.price %>" required>
            </div>
            <div>
                <label for="review">Review:</label>
                <textarea name="book[review]" id="review" rows="1" required><%= bookdata.review %></textarea>
            </div>
            <button>Add Book</button>
        </div>
    </form>
</body>
</html>
