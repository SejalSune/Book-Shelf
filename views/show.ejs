<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BookShelf</title>
</head>
<body>
    <h1>Details of Book</h1>
    <table border="1">
        <tr>
            <th>Title</th>
            <td><%= bookdata.title %></td>
        </tr>
        <tr>
            <th>Author</th>
            <td><%= bookdata.author %></td>
        </tr>
        <tr>
            <th>Language</th>
            <td><%= bookdata.language %></td>
        </tr>
        <tr>
            <th>Publisher</th>
            <td><%= bookdata.publisher %></td>
        </tr>
        <tr>
            <th>Publisher City</th>
            <td><%= bookdata.publisher_city %></td>
        </tr>
        <tr>
            <th>Publication Date</th>
            <td><%= bookdata.publication_date %></td>
        </tr>
        <tr>
            <th>ISBN</th>
            <td><%= bookdata.ISBN %></td>
        </tr>
    </table>

    <img src="<%= bookdata.coverpage %>" alt="" height="300px">

    <form action="/shelf/<%= bookdata._id %>?_method=DELETE" method="post">
        <button>Delete</button>
    </form>

    <form action='/shelf/<%= bookdata._id %>/edit' method="get">
        <button>Edit</button>
    </form>
    
</body>
</html>
